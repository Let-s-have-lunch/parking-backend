1. 회원정보를 저장하기 위한 엔티티 작성

2. 회원 로그인, 회원가입 작성

3. 주차장 정보를 가져오기 위해
   data.go.kr 에서 "전국주차장정보표준데이터" 신청

4. "전국주차장정보표준데이터"를 매 번 요청할 경우, 호출 한계 존재
   -> 표준데이터는 자주 변경되지 않는 정보이므로 내 데이터베이스에 저장
   -> 대신 sync로 최신 데이터를 유지할 수 있도록 고안

5. "전국주차장정보표준데이터"는 실시간 정보는 아님
   -> data.go.kr에서 "한국교통안전공단_주차장실시간정보" 신청

6. 두 공공API 데이터를 매칭해야하는 작업 필요

7. 이렇게 저장된 DB의 전국 주차장 정보는 만여건이 되므로,
   이걸 한꺼번에 프론트엔드에서 출력을 해버리면 브라우저가 버티지 못함
   -> 프론트엔드에서 화면에 보이는 지도의 정보를 기반으로 요청을 하도록
   -> 화면 영역 지도 사이즈 정보를 받아 백엔드에서 그 주차장만 응답

8. 최초 프로그램 시작 시에는 데이터베이스가 깨끗한 상태
   -> 주차장 정보를 미리 DB에 넣어두어야 할 필요 있음


9. "한국교통안전공단 주차장 실시간 정보" API는 "전국주차장정보표준데이터"와 별개의 관리 체계를 가짐
```
행정안전부(전국표준데이터)의 주차장 관리번호와 국토부/교통안전공단(실시간 데이터)의 주차장 관리번호는
서로 발급 주체와 체계가 달라서 절대 일치하지 않습니다.
 (행안부는 지자체 코드를 따르고, 교통안전공단은 자체 통합 시스템 코드를 씁니다.)
```

10. 한국교통안전공단 주차장 실시간 정보는 폐기...

11. 안산시에서는 주차장 정보 API를 제공하지 않음

12. 서울시 공영주차장 정보 API에서는 관리 이슈로 인하여 실시간 정보가 삭제되었음

13. 서울시 시영주차장 정보 API에서는 실시간 정보를 제공함

14. 물론 주차장ID 관리가 여기도 다름
- 전국정보에는 주소, 동경, 위도 등의 정보가 존재함 => 전국 정보를 DB에 저장
- 서울정보에는 주소만 존재함
- 주소를 비교하여 일치한 정보가 있는 주차장 정보에만 DB에 isSupported 칼럼의 값을 true로 하여
  프론트엔드에서 그 주차장에 대해 조회를 할 경우, 실시간 주차 정보를 확보하기로 함

15. 프로그램을 시작할 때 자동으로 "전국주차장정보표준데이터"와 "서울시 시영주차장 정보"를 갱신함

16. 주차장정보표준데이터는 프로그램 시작 때 갱신하지만, 서울시 시영주차장 정보는 5분 당 한번씩 캐시에 갱신함

17. 사실 이렇게 해도 서울시 시영주차장 실시간 API가 제공해주는 주차장 건 수는 122건 밖에 안됨....
- 청계5(평화시장)
- 창동역(서)
- 남산파출소